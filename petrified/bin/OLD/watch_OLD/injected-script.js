/*
var origin = window.location.origin;

// https -> wss, http -> ws
var URL = origin.replace(/^http/, "ws"); 
var webSocket = new WebSocket(URL);

webSocket.on("message", function (message)
{
    
});
*/